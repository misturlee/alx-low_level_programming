#include <stdio.h>
#include "main.h"

/**
 * main - to check the code.
 *
 * @c: the character to be checked
 * Return: 0 means success.
 */
int int _isupper(int c);
{
	char c;

	if (c <= 90)
	{
		return (1);
	}
	else if (c > 90)
	{
		return (0);
	}
	return (0);
}
